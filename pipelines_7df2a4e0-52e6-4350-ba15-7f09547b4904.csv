2023-08-22 16:37:08,"
from sklearn.pipeline import Pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression

pipe = Pipeline([
    ('imputer', SimpleImputer()),
    ('scaler', StandardScaler()),
    ('classifier', LogisticRegression(C=20.0, dual=False, penalty='l2', solver='lbfgs'))
])

pipe.fit(X_train, y_train)
",0.5497005988023952
2023-08-22 16:37:12,"
from sklearn.pipeline import Pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import StandardScaler
from sklearn.ensemble import ExtraTreesClassifier
from sklearn.kernel_approximation import RBFSampler

pipe = Pipeline([
    ('imputer', SimpleImputer()),
    ('sampler', RBFSampler(gamma=0.4)),
    ('classifier', ExtraTreesClassifier(bootstrap=False, criterion='entropy', max_features=0.6, 
                                        min_samples_leaf=3, min_samples_split=10, n_estimators=100))
])

pipe.fit(X_train, y_train)",0.5664670658682635
2023-08-22 16:37:15,"
from sklearn.pipeline import Pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import StandardScaler
from sklearn.naive_bayes import GaussianNB
from sklearn.kernel_approximation import RBFSampler

pipe = Pipeline([
    ('imputer', SimpleImputer()),
    ('sampler', RBFSampler(gamma=0.2)),
    ('classifier', GaussianNB())
])

pipe.fit(X_train, y_train)",0.5005988023952096
2023-08-22 16:39:57,"
from mlxtend.classifier import EnsembleVoteClassifier

model = EnsembleVoteClassifier(clfs=list_pipelines, voting='soft', weights=None, verbose=0, use_clones=True, fit_base_estimators=True)
model.fit(X_train, y_train)
",0.5233532934131736
