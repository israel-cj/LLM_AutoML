2023-08-22 16:28:15,"
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression

pipe = Pipeline([
    ('scaler', StandardScaler()),
    ('classifier', LogisticRegression(C=20.0, dual=False, penalty='l2', solver='lbfgs'))
])

pipe.fit(X_train, y_train)
",0.5580838323353293
2023-08-22 16:28:19,"from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.ensemble import ExtraTreesClassifier
from sklearn.kernel_approximation import RBFSampler

pipe = Pipeline([
    ('sampler', RBFSampler(gamma=0.25)),
    ('classifier', ExtraTreesClassifier(bootstrap=False, criterion='gini', max_features=0.4, min_samples_leaf=2, min_samples_split=10, n_estimators=100))
])

pipe.fit(X_train, y_train)",0.5808383233532934
2023-08-22 16:28:23,"from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.naive_bayes import GaussianNB
from sklearn.kernel_approximation import RBFSampler

pipe = Pipeline([
    ('sampler', RBFSampler(gamma=0.2)),
    ('classifier', GaussianNB())
])

pipe.fit(X_train, y_train)",0.5473053892215569
2023-08-22 16:28:26,"
from mlxtend.classifier import EnsembleVoteClassifier

# Create the Multi-Layer Stack Ensembling model
model = EnsembleVoteClassifier(clfs=list_pipelines, voting='hard', use_clones=False)

# Fit the model with X_train and y_train
model.fit(X_train, y_train)
",0.5688622754491018
