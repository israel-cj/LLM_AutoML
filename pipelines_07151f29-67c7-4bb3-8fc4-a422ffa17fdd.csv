2023-09-11 12:26:58,"
from sklearn.pipeline import make_pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression

pipe = make_pipeline(
    SimpleImputer(),
    StandardScaler(),
    LogisticRegression()
)

pipe.fit(X_train, y_train)
",0.6245373172363266
2023-09-11 12:27:18,"
from sklearn.pipeline import make_pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import MaxAbsScaler
from sklearn.tree import DecisionTreeClassifier

pipe = make_pipeline(
    SimpleImputer(),
    MaxAbsScaler(),
    DecisionTreeClassifier(criterion='entropy', max_depth=12, min_samples_leaf=10, min_samples_split=20)
)

pipe.fit(X_train, y_train)",0.5913006084158889
2023-09-11 12:27:40,"
from sklearn.pipeline import make_pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import Binarizer
from sklearn.linear_model import LogisticRegression

pipe = make_pipeline(
    SimpleImputer(),
    Binarizer(threshold=0.4),
    LogisticRegression(C=15.0, dual=False, penalty='l2', solver='lbfgs')
)

pipe.fit(X_train, y_train)",0.584244895358838
